# Core (tested in this build)
pandas>=2.0
networkx>=3.0
numpy>=1.24

# Dashboard (run locally — needs internet for map tiles)
streamlit>=1.30
folium>=0.15
streamlit-folium>=0.18

# Optional — real geospatial data + real road network pulls
geopandas>=0.14
shapely>=2.0
osmnx>=1.9

# Optional — ML vulnerability model upgrade (stretch goal, see vulnerability.py)
scikit-learn>=1.3
plotly>=5.18

fastapi>=0.110
uvicorn[standard]>=0.29
scipy>=1.11
